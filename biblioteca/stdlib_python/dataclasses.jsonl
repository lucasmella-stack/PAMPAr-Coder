{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass\nclass Student:\n    name: str\n    age: int\n    grades: list[float] = field(default_factory=list)\n\n    def average(self) -> float:\n        if not self.grades:\n            return 0.0\n        return sum(self.grades) / len(self.grades)\n\ns = Student(\"Alice\", 20, [9.0, 8.5, 9.5])\nprint(s.average())  # 9.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(p)          # Point(x=3.0, y=4.0)\nprint(p.x, p.y)   # 3.0 4.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nimport json\n\n@dataclass\nclass Product:\n    id: int\n    name: str\n    price: float\n    tags: list[str] = field(default_factory=list)\n\n    def to_json(self) -> str:\n        return json.dumps(self.__dict__, ensure_ascii=False)\n\n    @classmethod\n    def from_dict(cls, data: dict) -> 'Product':\n        return cls(**data)\n\np = Product(1, \"Laptop\", 1499.99, [\"tech\", \"computers\"])\nprint(p.to_json())", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass LogEntry:\n    message: str\n    level: str = \"INFO\"\n    timestamp: datetime = field(default_factory=datetime.now)\n    tags: list[str] = field(default_factory=list)\n\n    def __str__(self) -> str:\n        ts = self.timestamp.strftime(\"%H:%M:%S\")\n        return f\"[{ts}] [{self.level}] {self.message}\"\n\nentry = LogEntry(\"Server started\", \"INFO\", tags=[\"startup\"])\nprint(entry)  # [HH:MM:SS] [INFO] Server started", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Vector:\n    \"\"\"Vector inmutable — puede usarse como clave de dict o en sets.\"\"\"\n    x: float\n    y: float\n\n    def magnitude(self) -> float:\n        return (self.x**2 + self.y**2)**0.5\n\n    def __add__(self, other: 'Vector') -> 'Vector':\n        return Vector(self.x + other.x, self.y + other.y)\n\nv1 = Vector(3.0, 4.0)\nv2 = Vector(1.0, 2.0)\nprint(v1 + v2)          # Vector(x=4.0, y=6.0)\nprint(v1.magnitude())   # 5.0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Node:\n    \"\"\"Nodo de lista enlazada implementado con dataclass.\"\"\"\n    value: int\n    next: Optional['Node'] = field(default=None, repr=False)\n\ndef build_linked_list(values: list[int]) -> Optional[Node]:\n    if not values:\n        return None\n    head = Node(values[0])\n    curr = head\n    for v in values[1:]:\n        curr.next = Node(v)\n        curr = curr.next\n    return head\n\nhead = build_linked_list([1, 2, 3])\nprint(head.value)       # 1\nprint(head.next.value)  # 2", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass\nclass RGB:\n    red: int\n    green: int\n    blue: int\n\n    def to_hex(self) -> str:\n        return f\"#{self.red:02X}{self.green:02X}{self.blue:02X}\"\n\ncolor = RGB(255, 128, 0)\nprint(color.to_hex())           # #FF8000\nprint(asdict(color))            # {'red': 255, 'green': 128, 'blue': 0}\nprint(astuple(color))           # (255, 128, 0)\nr, g, b = astuple(color)\nprint(r, g, b)                  # 255 128 0", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Counter:\n    \"\"\"Contador con máximo configurable.\"\"\"\n    MAX_VALUE: ClassVar[int] = 100\n    value: int = 0\n\n    def increment(self, by: int = 1) -> None:\n        self.value = min(self.value + by, self.MAX_VALUE)\n\n    def reset(self) -> None:\n        self.value = 0\n\nc = Counter()\nc.increment(50)\nc.increment(70)  # No supera MAX_VALUE\nprint(c.value)   # 100", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n@dataclass\nclass BankAccount:\n    owner: str\n    balance: float = 0.0\n\n    def deposit(self, amount: float) -> None:\n        if amount <= 0:\n            raise ValueError(\"El monto debe ser positivo\")\n        self.balance += amount\n\n    def withdraw(self, amount: float) -> None:\n        if amount > self.balance:\n            raise ValueError(\"Saldo insuficiente\")\n        self.balance -= amount\n\n    def __str__(self) -> str:\n        return f\"Cuenta de {self.owner}: ${self.balance:.2f}\"\n\nacc = BankAccount(\"Alice\")\nacc.deposit(1000)\nacc.withdraw(250)\nprint(acc)  # Cuenta de Alice: $750.00", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass\n\n# ¿Por qué usar @dataclass en lugar de __init__ manual?\n# Genera automáticamente: __init__, __repr__, __eq__\n\n# Sin dataclass (verboso)\nclass PointManual:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n    def __repr__(self):\n        return f\"Point(x={self.x}, y={self.y})\"\n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\n# Con dataclass (limpio)\n@dataclass\nclass Point:\n    x: float\n    y: float\n\n# Equivalentes pero dataclass es mucho más conciso\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1 == p2)   # True (automático)\nprint(repr(p1))   # Point(x=1.0, y=2.0) (automático)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field, asdict\n\n@dataclass\nclass Config:\n    host: str = \"localhost\"\n    port: int = 8080\n    debug: bool = False\n    tags: list[str] = field(default_factory=list)\n\ncfg = Config(port=9000, tags=[\"api\", \"v1\"])\nprint(cfg)\n# Config(host='localhost', port=9000, debug=False, tags=['api', 'v1'])\n\n# Convertir a dict para serializar\nd = asdict(cfg)\nprint(d['port'])  # 9000", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "from dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass Task:\n    \"\"\"Tarea con prioridad — comparable por prioridad.\"\"\"\n    priority: int\n    name: str = field(compare=False)  # No entra en comparación\n    done: bool = field(default=False, compare=False)\n\ntasks = [Task(2, \"Email\"), Task(1, \"Bug fix\"), Task(3, \"Docs\")]\ntasks.sort()\nfor t in tasks:\n    print(f\"[{t.priority}] {t.name}\")\n# [1] Bug fix\n# [2] Email\n# [3] Docs", "source": "generated_templates", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "### Problem:\nYou are tasked with implementing a Python class that can parse a specific file format and extract metadata and strings from it. The file format is structured in a way that metadata is stored at the beginning of the file, followed by a section containing strings. Your task is to create a class with a method that can parse this file and extract the metadata and strings.\n\nYou are given a code snippet as a starting point:\n\n```python\nmetadata: Metadata\nstrings: Strings = field(default_factory=Strings)\n\n@classmethod\ndef parse_file(cls, path):\n```\n\nYour task is to complete the `parse_file` method in the class, ensuring that it correctly reads the file at the given `path`, extracts the metadata, and populates the `metadata` and `strings` fields accordingly.\n\nYour class should have the following structure:\n- A `Metadata` class that represents the metadata extracted from the file.\n- A `Strings` class that represents the strings extracted from the file.\n- A class with the provided code snippet, containing the `parse_file` method.\n\nYour solution should handle any potential errors that may occur during file parsing and should follow best practices for file handling and error management.\n\nWrite the complete implementation of the class and the `parse_file` method, ensuring that it correctly extracts the metadata and strings from the given file.\n\n### Solution:\n```python\nfrom dataclasses import dataclass, field\nimport re\n\n@dataclass\nclass Metadata:\n    # Define the structure of the Metadata class based on the actual metadata fields in the file\n    field1: str\n    field2: int\n    # Add more fields as per the actual metadata structure\n\n@dataclass\nclass Strings:\n    # Define the structure of the Strings class based on the actual string fields in the file\n    string_list: list\n    # Add more fields as per the actual string structure\n\nclass FileParser:\n    metadata: Metadata\n    strings: Strings = field(default_factory=Strings)\n\n    @classmethod\n    def parse_file(cls, path):\n        try:\n            with open(path, 'r') as file:\n                lines = file.readlines()\n                metadata_fields = lines[0].strip().split(',')  # Assuming metadata is comma-separated\n                metadata = Metadata(*metadata_fields)\n\n                string_list = []\n                for line in lines[1:]:\n                    string_list.extend(re.findall(r'\"([^\"]*)\"', line))  # Extract strings within double quotes\n\n                cls.metadata = metadata\n                cls.strings = Strings(string_list)\n        except FileNotFoundError:\n            print(f\"File not found at path: {path}\")\n        except Exception as e:\n            print(f\"An error occurred while parsing the file: {e}\")\n\n# Usage\nFileParser.parse_file('path_to_file.txt')\nprint(FileParser.metadata)\nprint(FileParser.strings)\n```\n\nIn this solution, we define the `Metadata` and `Strings` classes to represent the structure of the metadata and strings in the file. We then implement the `parse_file` method in the `FileParser` class, which reads the file, extracts the metadata and strings, and populates the respective fields. Error handling is included to manage potential file-related and parsing errors. Finally, we demonstrate the usage of the `parse_file` method and access the extracted metadata and strings.", "source": "Magicoder-OSS-75K", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "### Problem:\nYou are tasked with implementing a data preprocessing module for a natural language processing (NLP) project. The module will be responsible for processing code and natural language (NL) text data to prepare it for further analysis. Your task is to create a class that encapsulates the preprocessing arguments and their default values.\n\nYou are provided with a code snippet that defines a data class `PreprocessingArguments` using the `dataclass` decorator from the `dataclasses` module in Python. The class has two fields, `code_vocab_size` and `nl_vocab_size`, which represent the maximum sizes of the code vocabulary and NL vocabulary, respectively. The fields have default values of 50000 and 30000, and include metadata to provide help information.\n\nYour task is to complete the `PreprocessingArguments` class by adding the missing field for `max_sequence_length` with a default value of 200. Additionally, you need to create an instance of the `PreprocessingArguments` class with the default values and print the details of the instance.\n\nComplete the following tasks:\n1. Add the missing field `max_sequence_length` to the `PreprocessingArguments` class with a default value of 200.\n2. Create an instance of the `PreprocessingArguments` class with the default values for all fields.\n3. Print the details of the instance, including the values of all fields and their help metadata.\n\n### Solution:\n```python\nfrom dataclasses import dataclass, field\n\n@dataclass\nclass PreprocessingArguments:\n    \"\"\"Arguments for data preprocessing.\"\"\"\n\n    code_vocab_size: int = field(\n        default=50000,\n        metadata={'help': 'Maximum size of code vocab'}\n    )\n\n    nl_vocab_size: int = field(\n        default=30000,\n        metadata={'help': 'Maximum size of NL vocab'}\n    )\n\n    max_sequence_length: int = field(\n        default=200,\n        metadata={'help': 'Maximum sequence length'}\n    )\n\n# Create an instance of PreprocessingArguments with default values\npreprocessing_args = PreprocessingArguments()\n\n# Print the details of the instance\nprint(\"Preprocessing Arguments:\")\nprint(f\"Code Vocab Size: {preprocessing_args.code_vocab_size} - {preprocessing_args.__annotations__['code_vocab_size']['metadata']['help']}\")\nprint(f\"NL Vocab Size: {preprocessing_args.nl_vocab_size} - {preprocessing_args.__annotations__['nl_vocab_size']['metadata']['help']}\")\nprint(f\"Max Sequence Length: {preprocessing_args.max_sequence_length} - {preprocessing_args.__annotations__['max_sequence_length']['metadata']['help']}\")\n```\n\nOutput:\n```\nPreprocessing Arguments:\nCode Vocab Size: 50000 - Maximum size of code vocab\nNL Vocab Size: 30000 - Maximum size of NL vocab\nMax Sequence Length: 200 - Maximum sequence length\n```", "source": "Magicoder-OSS-75K", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "### Problem:\nYou are tasked with creating a Python class that represents a service task in a BPMN (Business Process Model and Notation) workflow. BPMN is a standard for modeling business processes, and a service task is a type of task that invokes a service or operation. Your task is to create a Python class that represents this service task, adhering to the given code snippet as a starting point.\n\nYour Python class should be named `ServiceTask` and should inherit from `TServiceTask`. It should be a data class and should have a nested class `Meta` with two attributes: `name` and `namespace`. The `name` attribute should be set to \"serviceTask\", and the `namespace` attribute should be set to \"http://www.omg.org/spec/BPMN/20100524/MODEL\".\n\nCreate the `ServiceTask` class according to the given requirements.\n\n### Solution:\n```python\nfrom dataclasses import dataclass\n\nclass TServiceTask:\n    pass\n\n@dataclass\nclass ServiceTask(TServiceTask):\n    class Meta:\n        name = \"serviceTask\"\n        namespace = \"http://www.omg.org/spec/BPMN/20100524/MODEL\"\n```\nIn the solution, we define the `TServiceTask` class as a placeholder, and then create the `ServiceTask` class as a data class with the specified nested `Meta` class containing the required attributes. This solution adheres to the given problem description and accurately represents a Python class for a service task in a BPMN workflow.", "source": "Magicoder-OSS-75K", "license": "open", "lang": "python", "tema": "dataclasses"}
{"text": "### Problem:\nYou are tasked with implementing a Python class that represents a solid property in the context of Geographic Markup Language (GML). The GML is an XML grammar for expressing geographical features. Your task is to create a class that adheres to the specifications of the SolidPropertyType and can be used in GML application schemas.\n\nYour task is to create a Python class named SolidProperty that represents a solid property according to the GML specifications. The class should have the following characteristics:\n- It should be a dataclass.\n- It should inherit from SolidPropertyType.\n- It should have a docstring that describes the purpose of the property element.\n\nYour implementation should adhere to the following specifications:\n- The class should be named SolidProperty and should inherit from SolidPropertyType.\n- The namespace for the class should be \"http://www.opengis.net/gml\".\n- The class should have a docstring that explains the purpose of the property element, which can be used by GML Application Schemas whenever a GML Feature has a property with a value that is substitutable for _Solid.\n\nYour task is to implement the SolidProperty class according to the given specifications.\n\n### Solution:\n```python\nfrom dataclasses import dataclass\nfrom bindings.csw.composite_solid_type import SolidPropertyType\n\n__NAMESPACE__ = \"http://www.opengis.net/gml\"\n\n@dataclass\nclass SolidProperty(SolidPropertyType):\n    \"\"\"This property element either references a solid via the XLink-attributes\n    or contains the solid element.\n\n    solidProperty is the predefined property which can be used by GML\n    Application Schemas whenever a GML Feature has a property with a\n    value that is substitutable for _Solid.\n    \"\"\"\n```\n\nThe provided solution implements the SolidProperty class according to the given specifications. It is a dataclass named SolidProperty that inherits from SolidPropertyType. The namespace is set to \"http://www.opengis.net/gml\", and the class has a docstring that explains the purpose of the property element as per the requirements.", "source": "Magicoder-OSS-75K", "license": "open", "lang": "python", "tema": "dataclasses"}
