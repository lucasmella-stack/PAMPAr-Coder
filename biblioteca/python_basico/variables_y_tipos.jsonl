{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "valores = [1, \"hola\", 3.14, True, None]\nfor v in valores:\n    print(f\"{v!r:<10} -> {type(v).__name__}\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x: int = 10\ny: float = 3.14\nz: str = \"hola\"\nw: bool = True\nprint(x, y, z, w)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Conversión de tipos\nentero = int(\"42\")\nflotante = float(\"3.14\")\ntexto = str(100)\nbool_val = bool(0)\nprint(entero, flotante, texto, bool_val)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "numero = 255\nprint(bin(numero))   # 0b11111111\nprint(oct(numero))   # 0o377\nprint(hex(numero))   # 0xff\nprint(int(\"ff\", 16)) # 255", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Variables múltiples\na, b, c = 1, 2, 3\nprint(a, b, c)  # 1 2 3\n\n# Swap\na, b = b, a\nprint(a, b)  # 2 1\n\n# Desempaquetado extendido\nprimero, *resto = [1, 2, 3, 4, 5]\nprint(primero, resto)  # 1 [2, 3, 4, 5]", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 42\ny = 3.14\nnombre = \"Python\"\nactivo = True\nvacio = None\nprint(type(x), type(y), type(nombre))", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "MAXIMO_INT = 2**63 - 1\nMINIMO_FLOAT = 1e-308\nINFINITO = float(\"inf\")\nNAN = float(\"nan\")\nprint(MAXIMO_INT)\nprint(MINIMO_FLOAT)\nimport math\nprint(math.isinf(INFINITO))  # True\nprint(math.isnan(NAN))       # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "edad = 25\naltura = 1.75\nprint(f\"Edad: {edad}, Altura: {altura:.2f}m\")", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "def es_numero(s: str) -> bool:\n    \"\"\"Verifica si un string puede convertirse a número.\"\"\"\n    try:\n        float(s)\n        return True\n    except ValueError:\n        return False\n\nprint(es_numero(\"3.14\"))  # True\nprint(es_numero(\"abc\"))   # False", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "# Operadores de comparación e identidad\na = [1, 2, 3]\nb = [1, 2, 3]\nc = a\nprint(a == b)   # True (igual valor)\nprint(a is b)   # False (distinto objeto)\nprint(a is c)   # True (mismo objeto)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "x = 5\nprint(isinstance(x, int))    # True\nprint(isinstance(x, float))  # False\nprint(type(x) is int)        # True", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "nombre = \"Lucas\"\nprint(nombre.upper())        # LUCAS\nprint(len(nombre))           # 5\nprint(nombre[0])             # L\nprint(nombre[-1])            # s\nprint(nombre[:3])            # Luc", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
{"text": "a = 10\nb = 3\nprint(a + b)   # 13\nprint(a - b)   # 7\nprint(a * b)   # 30\nprint(a / b)   # 3.333\nprint(a // b)  # 3 (división entera)\nprint(a % b)   # 1 (módulo)\nprint(a ** b)  # 1000 (potencia)", "source": "generated_templates", "license": "open", "lang": "python", "tema": "variables_y_tipos"}
